/// Enum representing the type of player backend to use.
enum PlayerType {
  /// VLC player backend (flutter_vlc_player)
  vlc,

  /// MediaKit player backend (media_kit)
  mediaKit,

  videoPlayer,
}
